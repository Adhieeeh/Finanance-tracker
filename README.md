# TypeScript Finance Tracker

A simple project demonstrating how to use **TypeScript** to handle **JSON** data without using external APIs and also this is my first ever typscript and json program.

 How to Run
1. Install dependencies: `npm install -g ts-node typescript`
2. Run the app: `ts-node app.ts`


github repo:(https://github.com/Adhieeeh/Finanance-tracker.git)
